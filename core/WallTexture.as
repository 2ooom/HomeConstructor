package core
{
	public class WallTexture extends TextureBase
	{
		/**
		 * Constructor. Creates new instanse of <code>WallTexture</code> image.
		 *
		 * @param	node	XML node from 'config.xml' in correct format.
		 */
		public function WallTexture(node:XML) {
			super(node);
		}
	}
}