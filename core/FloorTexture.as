package core
{
	public class FloorTexture extends TextureBase
	{
		/**
		 * Constructor. Creates new instanse of <code>FloorTexture</code> image.
		 *
		 * @param	node	XML node from 'config.xml' in correct format.
		 */
		public function FloorTexture(node:XML) {
			super(node);
		}
	}
}